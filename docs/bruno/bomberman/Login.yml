info:
  name: Login
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_URI}}/player/login"
  body:
    type: json
    data: |-
      {
        "username": "srfr",
        "password": "bananabanana"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
