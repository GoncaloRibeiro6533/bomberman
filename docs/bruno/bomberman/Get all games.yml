info:
  name: Get all games
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URI}}/game/all"
  headers:
    - name: Authorization
      value: ""
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
