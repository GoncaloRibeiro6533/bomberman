package com.evolution.domain.cell

import com.evolution.domain.PositiveNumber

/**
 * Represents a line in a grid
 *
 * @param value A positive number that indicates the index of the line
 */
final case class Line(value: PositiveNumber)
